# ═══════════════════════════════════════════════════════════════════════════
# SERVICES SIMPLIFIÉS (RECOMMANDÉS)
# ═══════════════════════════════════════════════════════════════════════════

start_heating_cycle:
  name: Démarrer Cycle Chauffage
  description: >
    Démarre un nouveau cycle de chauffage journalier. 
    Place la machine à états en mode HEATING_ON (chauffage actif).
    Utilisez ce service pour forcer le début d'un nouveau cycle.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

stop_heating:
  name: Arrêter Chauffage
  description: >
    Arrête le chauffage et démarre la phase de surveillance nocturne.
    Active la détection du lag de température puis lance les calculs de relance.
    Transitions: HEATING_ON → DETECTING_LAG → MONITORING.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

start_recovery:
  name: Démarrer Relance
  description: >
    Démarre la phase de relance du chauffage pour atteindre la température cible 
    à l'heure de réveil. Calcule automatiquement RCth et active le calcul de RPth.
    Transitions: MONITORING → RECOVERY → HEATING_PROCESS.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

end_recovery:
  name: Terminer Relance
  description: >
    Termine la phase de relance (consigne atteinte ou heure cible atteinte).
    Calcule RPth et retourne en mode chauffage normal.
    Transition: HEATING_PROCESS → HEATING_ON.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

get_state:
  name: Obtenir État
  description: >
    Retourne l'état actuel de la machine à états avec tous les détails 
    (températures, heures, coefficients thermiques, modes actifs).
    Utile pour le debug et la supervision.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

# ═══════════════════════════════════════════════════════════════════════════
# SERVICES UTILITAIRES
# ═══════════════════════════════════════════════════════════════════════════

reset_learning:
  name: Réinitialiser Apprentissage
  description: >
    Réinitialise tous les coefficients thermiques appris (RCth, RPth et leurs 
    variantes vent) aux valeurs par défaut (50). Utilisez ce service si les 
    valeurs apprises sont devenues inexactes, par exemple après des modifications 
    de l'habitation ou de l'isolation.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

trigger_calculation:
  name: Déclencher Calcul
  description: >
    Déclenche immédiatement un nouveau calcul de l'heure de relance.
    Utile après avoir modifié des paramètres pour voir l'effet immédiatement 
    sans attendre le prochain calcul automatique.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

# ═══════════════════════════════════════════════════════════════════════════
# SERVICES HISTORIQUES (CONSERVÉS POUR COMPATIBILITÉ - DÉCONSEILLÉS)
# ═══════════════════════════════════════════════════════════════════════════

calculate_recovery_time:
  name: "[Ancien] Calculate Recovery Time"
  description: >
    ⚠️ Service historique conservé pour compatibilité. Utilisez plutôt 'trigger_calculation'.
    Calcule l'heure de démarrage de la relance basé sur les paramètres thermiques.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

calculate_recovery_update_time:
  name: "[Ancien] Calculate Recovery Update Time"
  description: >
    ⚠️ Service historique conservé pour compatibilité. Utilisation interne.
    Calcule quand la prochaine mise à jour de l'heure de relance doit avoir lieu.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

calculate_rcth_fast:
  name: "[Ancien] Calculate RCth Fast"
  description: >
    ⚠️ Service historique conservé pour compatibilité. Utilisation interne.
    Calcule l'évolution dynamique de RCth avant le démarrage de la relance.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

on_heating_stop:
  name: "[Ancien] On Heating Stop"
  description: >
    ⚠️ Service historique conservé pour compatibilité. Utilisez plutôt 'stop_heating'.
    Enregistre l'arrêt du chauffage avec températures et timestamps.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

on_recovery_start:
  name: "[Ancien] On Recovery Start"
  description: >
    ⚠️ Service historique conservé pour compatibilité. Utilisez plutôt 'start_recovery'.
    Démarre la phase de relance, enregistre les températures et active le calcul RPth.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:

on_recovery_end:
  name: "[Ancien] On Recovery End"
  description: >
    ⚠️ Service historique conservé pour compatibilité. Utilisez plutôt 'end_recovery'.
    Termine la phase de relance et calcule RPth.
  fields:
    entry_id:
      name: Entry ID
      description: >
        L'ID de l'instance SmartHRT (multi-instance). Si vous avez plusieurs instances, 
        spécifiez l'entry_id pour cibler une instance spécifique.
      required: false
      advanced: true
      example: "abc123def456"
      selector:
        text:
